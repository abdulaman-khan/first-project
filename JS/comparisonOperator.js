// comparison operator
let a=20;
let b="10"
let c="20"

console.log(a==c) // == will only check for the values

console.log(a===c) // === will only check both the values and data type

console.log(a!=c) // != will only check for the values

console.log(a!==c)// !== will check both the data types and values

// ternory operator

let age=18

console.log((age>=18)? "Eligible for vote" : " not eligible for vote")